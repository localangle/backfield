[project]
name = "backfield-observability"
version = "0.1.0"
description = "Runtime identity and CloudWatch EMF metric helpers for Backfield"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/backfield_observability"]
