[project]
name = "backfield-entities"
version = "0.1.0"
description = "Entity domain helpers (catalog, canonical matching, ingest bridge)"
requires-python = ">=3.11"
dependencies = [
    "number-parser>=0.3.2,<0.4",
    "pydantic>=2.0.0",
    "sqlmodel>=0.0.16",
    "backfield-db",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/backfield_entities"]
