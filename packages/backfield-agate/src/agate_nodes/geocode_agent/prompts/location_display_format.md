Return only `{"location":"<one line>"}` — public **Location** (not the full mailing line in formatted_address).

Use **`type`**, **`q`**, **`formatted_address`**, **`components_json`**, **`original_text`**, and **`geocode_hints`** (hints may be `(none)`).

1. Strip trailing `US`/`USA`/`United States` for US domestic. Keep country only if clearly non-US, or `type` is `region_country`, or feature is national/multi-state (then end `, US`, never `USA`).
2. **Casing**: **Title-case** place names per comma segment. **Uppercase** any comma segment that is exactly **two letters** and is a valid **US (incl. DC) or Canadian** subdivision code. **Remove consecutive duplicate segments** when two comma-separated parts are the **same** toponym so the line reads **City, ST** not **City, City, ST**. **Lowercase** short joiners (**of, and, the, or, …**) except the **first word** of each segment. Fix **letter + apostrophe + name** tokens (capitalize after `'`; not English contractions). Trailing national `US` stays uppercase. **Dotted initialisms** (**U.S.**, **D.C.**, **N.Y.**, **B.B.**) and similar **must** keep **every letter** uppercase; **Ph.D.**-style abbreviations use **mixed** casing (**Ph.D.**); short **&** forms like **AT&T** use **all caps** on each side of **&**.
3. **Order**: Prefer conventional **head toponym before generic placetype** when `q`, `formatted_address`, and `components_json` show they name the **same** incorporated place. Do not invent or merge unrelated segments.
4. **Neighborhood / district disambiguation**: For `place`, `point`, `address`, or similar, when **`q` plus city and state** would be **ambiguous** (same brand or generic venue could exist in **many** neighborhoods in that city), read **`original_text`** and **`geocode_hints`**. If they clearly name a **specific** neighborhood, district, corridor, or defensible in-story anchor (by **actual** placename, not the generic word *neighborhood*), insert **only that real placename** between the feature and the city, e.g. **`{Feature}, {fine-grained placename}, {City}, {ST}`**. Prefer **`components_json`** neighborhood/district **name** fields when they **agree** with the story; **never** invent a finer-grained name not supported by text or hints. **Never** output the generic word **Neighborhood** (or **District**) as its **own** comma segment—that is a type label, not a toponym.
5. In-city (when no extra disambiguation is needed): `place`,`address`,`neighborhood`,`district`,`intersection_*`,`street_road`,`region_city`,`point`,… → `{Feature}, {City}, {ST}` (US → 2-letter ST).
6. State-level: `city`,`town`,`county`,`region`,`area`,`region_*` except `region_city` → `{Feature}, {State}`; if `type` is `state` → **full state name** only (no 2-letter code in that slot).
7. `natural`: multi-state / park-scale → `{Name}, US`; single-city → `{Name}, {City}, {ST}`.
8. Address/place: if `formatted_address` leads with a **formal POI/venue** then street → location = venue+city+state only (no street; street stays in formatted_address). Else follow (5) without pasting the full address line.

Trim fields; do not invent geography. If `formatted_address` is empty, derive conservatively from `q` and `type`.
