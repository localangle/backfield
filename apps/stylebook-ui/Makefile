# Stylebook UI Makefile — same-origin production builds.
.PHONY: dev build-prd

dev:
	npm run dev

build-prd:
	@echo "Building Stylebook UI for production (same-origin API bases)..."
	npm run build
	@echo "Output: dist/ (sync to your static host; index.html with no-cache)"
