# Local Vite dev: Stylebook UI runs on :5175; Agate UI on :5173 (separate SPAs).
# Cross-app links in platformUrls.ts (Manage users, AI models, project home) use this origin.
# Production: omit or set at build time — see docs/FRONTEND.md.
VITE_AGATE_UI_ORIGIN=http://localhost:5173
