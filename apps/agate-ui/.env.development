# Local Vite dev: Agate UI runs on :5173; Stylebook UI on :5175 (separate SPAs).
# Cross-app links in platformUrls.ts use these origins (processed-item → Stylebook handoff, sidebar).
# Production: omit or set at build time — see DEPLOY.md and docs/FRONTEND.md.
VITE_STYLEBOOK_UI_ORIGIN=http://localhost:5175
